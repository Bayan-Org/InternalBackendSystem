name: Deploy Backend

on:
  push:
    branches:
      - main
jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - name: Deploy
        run: |
          cd /srv/projects/InternalBackendSystem
          git pull origin main
          docker compose up -d --build
          docker image prune -f
